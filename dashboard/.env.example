# Only needed for `npm run dev` / `npm run preview` OUTSIDE Docker, where
# there's no nginx proxy in front of the API. Inside docker-compose, the
# dashboard's nginx.conf already proxies /api/* to the backend container,
# so this can stay unset there.
VITE_API_BASE_URL=http://localhost:8000/api/v1
